---
import TerminalHero from './TerminalHero';

interface Props {
  techCount: number;
  essaysCount: number;
}

const { techCount, essaysCount } = Astro.props;
---

<TerminalHero client:load techCount={techCount} essaysCount={essaysCount} />
